/**
 * Options for resolving an expression during code generation.
 *
 * @property resolver - The name of the resolver function to use for resolving the expression.
 */
export type ResolveExpressionOptions = { resolver?: string }